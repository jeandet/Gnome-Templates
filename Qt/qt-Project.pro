QT       += core gui

greaterThan(QT_MAJOR_VERSION, 4): QT += widgets

TARGET = XXX
TEMPLATE = lib #app

#LIBS += -lfl

#QMAKE_CXXFLAGS += -fopenmp
#QMAKE_LFLAGS += -fopenmp

MOC_DIR      = moc
RCC_DIR      = resources
OBJECTS_DIR  = obj
DESTDIR      = bin

#INCLUDEPATH += 

SOURCES += 


HEADERS  += 

OTHER_FILES += 
